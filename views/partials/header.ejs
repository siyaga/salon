<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>

    <link rel="stylesheet" href="/css/bootstrap.min.css" />

    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />

    <style>
      body {
        font-family: "Poppins", sans-serif;
        /* Latar belakang yang sangat halus */
        background-color: #f9fbfd;
      }

      /* Warna Brand (Ungu) */
      .brand-icon {
        font-size: 3rem;
        color: #6c5ce7;
      }
      .display-1-brand {
        font-weight: 700;
        color: #6c5ce7;
        font-size: 6rem;
      }
      .btn-primary {
        background-color: #6c5ce7;
        border-color: #6c5ce7;
      }
      .btn-primary:hover {
        background-color: #5a4bd9;
        border-color: #5a4bd9;
      }

      /* Style Kartu yang Konsisten */
      .card {
        /* Kita gunakan shadow & border di utility class */
        border-radius: 1rem; /* Lebih bulat & modern */
      }

      /* (BARU) Style untuk Status Member (Card Radio Button) */
      .status-radio {
        display: none; /* Sembunyikan radio button aslinya */
      }

      .status-check-label {
        display: block;
        border: 2px solid #e0e0e0; /* Border abu-abu netral */
        border-radius: 0.75rem; /* Samakan dengan card */
        padding: 1.5rem 1rem;
        text-align: center;
        cursor: pointer;
        transition: all 0.2s ease-in-out;
        height: 100%; /* (BARU) Agar tingginya sama */
      }

      .status-check-label:hover {
        background-color: #f8f9fa; /* Warna hover halus */
      }

      /* (BARU) Style saat dipilih (menggunakan warna brand Anda) */
      .status-radio:checked + .status-check-label {
        border-color: #6c5ce7; /* Warna brand Anda */
        background-color: #f4f2ff; /* Warna ungu yang sangat muda */
        box-shadow: 0 4px 10px rgba(108, 92, 231, 0.2);
        transform: translateY(-2px);
      }

      .status-check-label .brand-icon {
        font-size: 2.5rem; /* Sedikit lebih kecil agar pas */
        color: #6c5ce7; /* Pastikan warnanya konsisten */
      }
    </style>
  </head>
  <body class="bg-light">
    <div class="container-fluid mt-4 mb-5 px-md-4">
